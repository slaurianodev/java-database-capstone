name: Frontend Lint

on:
  push:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install linters
        run: |
          npm install -g htmlhint
          npm install -g stylelint stylelint-config-standard
          npm install -g eslint

      - name: Lint HTML
        run: |
          htmlhint "src/main/resources/static/assets/pages/**/*.html" || true

      - name: Lint CSS
        run: |
          stylelint "src/main/resources/static/assets/css/**/*.css" || true

      - name: Lint JavaScript
        run: |
          eslint "src/main/resources/static/assets/js/**/*.js" || true